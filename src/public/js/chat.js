


/*let chatslist = document.querySelector('#messages-list')

  document.querySelector('#messages-list').textContent = 'No hay mensajes enviados'
  let chathtml = ''
  data.forEach(message => {
      message.forEach( key => {
        chathtml += `
        <li> ${key.user}: escribio: " ${key.message} "
        <br>`
      })
  });
  
  chatslist.innerHTML = chathtml*/
  /*const editButtons = document.querySelectorAll('#btn-edit');
  editButtons.forEach((editButton) => {
    editButton.addEventListener('click', () => {
      try{

      }catch(e){
        console.log(e)
      }
      const title = document.querySelector('[name="title"]').value;
      const description = document.querySelector('[name="description"]').value;
      const price = document.querySelector('[name="price"]').value;
      const code = document.querySelector('[name="code"]').value;
      const stock = document.querySelector('[name="stock"]').value;

      const editedProduct = {
        //id: getNextId(), 
        title,
        description,
        price,
        code,
        stock,
      };

      socket.emit('client:productedited', editedProduct);
    });
  })*/

